# Phase 1 测试用例文档

> 项目：Ontology Hub
> 版本：Phase 1
> 编写人：QA Engineer
> 日期：2026-02-12
> 参考设计稿：ontology.pen（ULEA3, RPFoD, JL0QP）
> 参考验收标准：产品经理提供的 Phase 1 验收标准

---

## 一、Register Page（注册页面，路由：`/register`）

### 1.1 页面布局与展示

| 编号 | 用例名称 | 前置条件 | 测试步骤 | 预期结果 |
|------|---------|---------|---------|---------|
| REG-001 | 页面路由正确渲染 | 未登录状态 | 访问 `/register` 路由 | 页面居中显示注册卡片，包含 Logo（"Ontology"）、标题（"Create Account"）、副标题（"Get started with your free account"） |
| REG-002 | 表单字段完整性 | 进入注册页 | 检查表单字段 | 包含 4 个字段：Full Name、Email、Password、Confirm Password，每个字段有标签和占位文字 |
| REG-003 | 占位文字正确 | 进入注册页 | 查看各输入框占位文字 | Full Name: "Enter your full name"、Email: "Enter your email"、Password: "Create a password"、Confirm Password: "Confirm your password" |
| REG-004 | 注册按钮展示 | 进入注册页 | 查看注册按钮 | 显示紫色主色调 "Create Account" 按钮，宽度撑满卡片 |
| REG-005 | 条款文字展示 | 进入注册页 | 查看条款区域 | 显示 "By signing up, you agree to our Terms and Privacy Policy" |
| REG-006 | 分隔线展示 | 进入注册页 | 查看分隔区域 | 显示左右横线中间 "or" 的分隔线 |
| REG-007 | 登录跳转链接 | 进入注册页 | 查看底部链接 | 显示 "Already have an account? Sign In"，其中 "Sign In" 为主色调可点击链接 |
| REG-008 | 与 LoginPage 视觉一致性 | 进入注册页 | 对比注册页与登录页整体风格 | 暗色主题、紫色主色调、卡片样式、字体与 LoginPage 保持一致 |

### 1.2 表单验证

| 编号 | 用例名称 | 前置条件 | 测试步骤 | 预期结果 |
|------|---------|---------|---------|---------|
| REG-100 | 全部为空提交 | 进入注册页 | 不填写任何字段，点击 "Create Account" | 所有必填字段显示错误提示 |
| REG-101 | Full Name 为空 | 进入注册页 | 仅不填 Full Name，其他正确填写，提交 | Full Name 字段提示必填 |
| REG-102 | Email 为空 | 进入注册页 | 仅不填 Email，其他正确填写，提交 | Email 字段提示必填 |
| REG-103 | Password 为空 | 进入注册页 | 仅不填 Password，其他正确填写，提交 | Password 字段提示必填 |
| REG-104 | Confirm Password 为空 | 进入注册页 | 仅不填 Confirm Password，其他正确填写，提交 | Confirm Password 字段提示必填 |
| REG-105 | Email 格式无效 | 进入注册页 | 输入无效 Email（如 "abc", "abc@", "@.com"），提交 | Email 字段显示格式错误提示 |
| REG-106 | Email 格式有效 | 进入注册页 | 输入有效 Email（如 "user@example.com"），提交 | Email 字段无格式错误 |
| REG-107 | 密码不一致 | 进入注册页 | Password 输入 "Abc12345"，Confirm Password 输入 "Abc12346" | Confirm Password 字段提示密码不一致 |
| REG-108 | 密码一致 | 进入注册页 | Password 和 Confirm Password 均输入 "Abc12345" | 无密码不一致错误 |
| REG-109 | 密码强度不足 | 进入注册页 | 输入弱密码（如 "123", "aaa"） | 提示密码强度不足或不满足要求 |
| REG-110 | Full Name 长度边界 | 进入注册页 | 输入极短名称（1 字符）和极长名称（256+ 字符） | 短名称应可接受或提示最小长度；超长名称应被截断或提示 |
| REG-111 | Email 已被注册 | 进入注册页 | 使用已存在的 Email 注册 | 提示该 Email 已被注册 |

### 1.3 注册流程

| 编号 | 用例名称 | 前置条件 | 测试步骤 | 预期结果 |
|------|---------|---------|---------|---------|
| REG-200 | 成功注册 | 进入注册页 | 正确填写所有字段（唯一 Email、匹配密码），点击 "Create Account" | 注册成功（模拟），跳转至 `/login` 页面 |
| REG-201 | 注册中加载状态 | 进入注册页 | 提交注册表单 | 按钮显示加载状态（loading），防止重复提交 |
| REG-202 | 网络异常处理 | 断开网络 | 提交注册表单 | 显示网络错误提示，不丢失表单数据 |
| REG-203 | 服务端错误处理 | 服务端返回 500 | 提交注册表单 | 显示通用错误提示（如 "Registration failed, please try again"） |
| REG-204 | Sign In 链接跳转 | 进入注册页 | 点击 "Sign In" 链接 | 跳转至 `/login` 页面 |
| REG-205 | Terms 链接跳转 | 进入注册页 | 点击 "Terms" 或 "Privacy Policy" 链接 | 跳转至对应的条款/隐私政策页面 |
| REG-206 | 防止重复提交 | 注册表单填写完成 | 快速连续点击 "Create Account" 多次 | 仅发送一次请求 |

### 1.4 安全性

| 编号 | 用例名称 | 前置条件 | 测试步骤 | 预期结果 |
|------|---------|---------|---------|---------|
| REG-300 | 密码字段掩码 | 进入注册页 | 在 Password / Confirm Password 输入内容 | 输入内容以掩码（•••）形式显示 |
| REG-301 | XSS 防护 | 进入注册页 | 在 Full Name 中输入 `<script>alert('xss')</script>` | 输入被转义，不执行脚本 |
| REG-302 | SQL 注入防护 | 进入注册页 | 在 Email 中输入 `' OR 1=1 --` | 输入被正确处理，无注入风险 |

---

## 二、User Detail（用户详情页，路由：`/user-management/:userId`）

> 注意：Phase 1 使用 mock 数据渲染

### 2.1 页面布局与导航

| 编号 | 用例名称 | 前置条件 | 测试步骤 | 预期结果 |
|------|---------|---------|---------|---------|
| UD-001 | 页面路由正确渲染 | 已登录 | 访问 `/user-management/1` 路由 | 页面使用 MainLayout（Sidebar + Content）布局，左侧边栏（280px）+ 右侧主内容区 |
| UD-002 | 侧边栏导航项 | 进入用户详情 | 查看侧边栏菜单 | 包含：User Management、Roles & Permissions、API Keys、Audit Log |
| UD-003 | 面包屑导航 | 进入用户详情 | 查看顶部面包屑 | 显示 "Settings > User Management > John Doe"，前两级可点击 |
| UD-004 | 操作按钮展示 | 进入用户详情 | 查看顶部右侧按钮 | 显示 "Edit" 按钮（带铅笔图标）和 "Delete" 按钮（红色，带垃圾桶图标） |

### 2.2 用户信息卡片

| 编号 | 用例名称 | 前置条件 | 测试步骤 | 预期结果 |
|------|---------|---------|---------|---------|
| UD-100 | 用户头像展示 | 进入用户详情 | 查看用户头像 | 显示圆形头像，内含用户名首字母缩写（如 "JD"），紫色背景 |
| UD-101 | 用户基本信息 | 进入用户详情 | 查看用户名和邮箱 | 显示用户名（"John Doe"）和邮箱（"john.doe@company.com"） |
| UD-102 | 用户角色标签 | 进入用户详情 | 查看角色标签 | 显示角色徽章（如 "Admin"），带图标 |
| UD-103 | 状态信息展示 | 进入用户详情 | 查看状态区域 | 显示 Status（Active/Inactive）、Joined 日期、Last Active 时间 |
| UD-104 | 状态为 Active | 用户状态为活跃 | 查看 Status 字段 | 显示绿色 "Active" 状态标签 |
| UD-105 | 分隔线展示 | 进入用户详情 | 查看用户卡片内部 | 头像区域与详情区域间有分隔线 |

### 2.3 已分配域（Assigned Domains）

| 编号 | 用例名称 | 前置条件 | 测试步骤 | 预期结果 |
|------|---------|---------|---------|---------|
| UD-200 | 域卡片标题 | 进入用户详情 | 查看域区域标题 | 显示 "Assigned Domains" 标题、域图标、数量徽章（如 "3"） |
| UD-201 | 添加域按钮 | 进入用户详情 | 查看域区域右上角 | 显示 "Add Domain" 按钮，带 + 图标 |
| UD-202 | 域卡片列表 | 进入用户详情 | 查看域列表 | 水平展示多个域卡片（如 Enterprise、Healthcare、Finance） |
| UD-203 | 域卡片信息 | 进入用户详情 | 查看单个域卡片 | 包含域名、域图标、统计数据（Classes 数、Relations 数） |
| UD-204 | 选中域高亮 | 进入用户详情 | 查看域卡片样式 | 当前选中的域有紫色描边高亮（如 Enterprise），其他为默认描边 |
| UD-205 | 域卡片点击 | 进入用户详情 | 点击某个域卡片 | 可选中该域或跳转至域详情 |
| UD-206 | 添加域功能 | 进入用户详情 | 点击 "Add Domain" 按钮 | 弹出域选择对话框或跳转至域分配页面 |

### 2.4 编辑与删除功能

| 编号 | 用例名称 | 前置条件 | 测试步骤 | 预期结果 |
|------|---------|---------|---------|---------|
| UD-300 | 点击编辑按钮 | 进入用户详情 | 点击 "Edit" 按钮 | 进入编辑模式或跳转至编辑页面，可修改用户信息 |
| UD-301 | 编辑用户名 | 进入编辑模式 | 修改用户名并保存 | 用户名更新成功，页面显示新名称 |
| UD-302 | 编辑邮箱 | 进入编辑模式 | 修改邮箱并保存 | 邮箱更新成功（可能需要验证） |
| UD-303 | 编辑角色 | 进入编辑模式 | 修改用户角色并保存 | 角色更新成功，角色标签更新 |
| UD-304 | 取消编辑 | 进入编辑模式 | 点击取消 | 退出编辑模式，数据恢复原样 |
| UD-305 | 点击删除按钮 | 进入用户详情 | 点击 "Delete" 按钮 | 弹出确认对话框，提示 "确认删除该用户？" |
| UD-306 | 确认删除 | 删除确认对话框 | 点击确认删除 | 用户被删除，跳转回用户管理列表页 |
| UD-307 | 取消删除 | 删除确认对话框 | 点击取消 | 关闭对话框，用户数据不变 |

### 2.5 Mock 数据渲染

| 编号 | 用例名称 | 前置条件 | 测试步骤 | 预期结果 |
|------|---------|---------|---------|---------|
| UD-500 | Mock 数据完整加载 | 进入用户详情 | 查看页面所有数据区域 | 所有区域均使用 mock 数据正确渲染，无空白或 undefined 显示 |
| UD-501 | 不同 userId 路由 | 已登录 | 分别访问 `/user-management/1`、`/user-management/2` | 页面能根据 userId 参数正确渲染（或统一使用同一份 mock 数据） |

### 2.6 与列表页衔接

| 编号 | 用例名称 | 前置条件 | 测试步骤 | 预期结果 |
|------|---------|---------|---------|---------|
| UD-400 | 从列表页进入 | 用户管理列表页 | 点击某用户行/卡片 | 正确跳转到该用户的详情页，数据一致 |
| UD-401 | 面包屑返回列表 | 用户详情页 | 点击面包屑 "User Management" | 返回用户管理列表页 |
| UD-402 | 删除后列表更新 | 用户详情页 | 删除用户后返回列表 | 列表中不再包含已删除的用户 |
| UD-403 | 编辑后列表同步 | 用户详情页 | 编辑用户名后返回列表 | 列表中显示更新后的用户名 |

---

## 三、Global Search（全局搜索页，路由：`/search`）

> 注意：Phase 1 使用 mock 数据渲染，搜索结果至少 6 条，涵盖不同类型

### 3.1 页面布局与导航

| 编号 | 用例名称 | 前置条件 | 测试步骤 | 预期结果 |
|------|---------|---------|---------|---------|
| GS-001 | 页面路由正确渲染 | 已登录 | 访问 `/search` 路由 | 左侧边栏（280px）含完整导航 + 右侧搜索区域 |
| GS-002 | 面包屑导航 | 进入全局搜索 | 查看面包屑 | 显示 "Tools > Global Search" |
| GS-003 | Filters 按钮 | 进入全局搜索 | 查看顶部右侧 | 显示 "Filters" 按钮，带滑块图标 |
| GS-004 | 侧边栏导航分组 | 进入全局搜索 | 查看侧边栏 | 包含域选择、ONTOLOGY 分组（Knowledge Graph、Classes、Relations、Properties、Instances）、TOOLS 分组（SPARQL Query、Import/Export） |

### 3.2 搜索栏

| 编号 | 用例名称 | 前置条件 | 测试步骤 | 预期结果 |
|------|---------|---------|---------|---------|
| GS-100 | 搜索栏展示 | 进入全局搜索 | 查看搜索栏 | 显示搜索图标、占位文字 "Search across classes, relations, properties, instances..."、快捷键提示 "⌘K" |
| GS-101 | 搜索栏获取焦点 | 进入全局搜索 | 点击搜索栏 | 搜索栏获得焦点，可输入文字 |
| GS-102 | 键盘快捷键触发 | 页面任意位置 | 按 ⌘K（Mac）/ Ctrl+K（Windows） | 搜索栏获得焦点 |
| GS-103 | 输入搜索关键词 | 搜索栏获得焦点 | 输入 "Person" | 触发搜索，显示匹配结果 |
| GS-104 | 清空搜索 | 已有搜索关键词 | 清空搜索栏内容 | 恢复默认状态或显示全部结果 |
| GS-105 | 搜索防抖 | 搜索栏获得焦点 | 快速输入多个字符 | 不会为每个字符都发送请求，有合理的防抖延迟 |

### 3.3 筛选器（Filter Row）

| 编号 | 用例名称 | 前置条件 | 测试步骤 | 预期结果 |
|------|---------|---------|---------|---------|
| GS-200 | 筛选器完整性 | 进入全局搜索 | 查看筛选器 | 显示 "Filter:" 标签和 7 个筛选按钮：All、Classes、Relations、Properties、Instances、Axioms |
| GS-201 | 默认选中 All | 进入全局搜索 | 查看筛选器默认状态 | "All" 按钮为选中状态（紫色填充），其他为未选中（描边样式） |
| GS-202 | 切换筛选类型 | 进入全局搜索 | 点击 "Classes" 筛选按钮 | "Classes" 变为选中状态，"All" 取消选中，搜索结果仅显示 Class 类型 |
| GS-203 | 切换回 All | 已选中 "Classes" | 点击 "All" | "All" 恢复选中，显示所有类型结果 |
| GS-204 | 各筛选类型生效 | 有搜索结果 | 分别点击每个筛选按钮 | 对应 Classes 只显示 Class 类型（如 Person, Organization），Relations 只显示 Relation（如 hasEmployee），Properties 只显示 Property（如 hasName），Instances 只显示 Instance（如 John Smith），Axioms 只显示 Axiom 类型 |
| GS-205 | 筛选后计数更新 | 有搜索结果 | 切换筛选类型 | "Showing X of Y results" 数量根据筛选类型更新 |

### 3.4 搜索结果展示

| 编号 | 用例名称 | 前置条件 | 测试步骤 | 预期结果 |
|------|---------|---------|---------|---------|
| GS-300 | 结果列表标题 | 搜索有结果 | 查看结果区域 | 显示 "Search Results" 标题和结果计数 "Showing X of Y results" |
| GS-301 | 结果项结构 | 搜索有结果 | 查看单个搜索结果 | 每个结果包含：类型图标（颜色区分）、名称、类型徽章、URI、描述文字 |
| GS-302 | Class 类型展示 | 搜索含 Class 结果 | 查看 Class 结果项 | 图标为 boxes（紫色），徽章为 "Class"（紫色背景），显示 URI（如 "ent:Person"） |
| GS-303 | Relation 类型展示 | 搜索含 Relation 结果 | 查看 Relation 结果项 | 图标为 link（绿色），徽章为 "Relation"，显示 URI |
| GS-304 | Property 类型展示 | 搜索含 Property 结果 | 查看 Property 结果项 | 图标为 list（橙色），徽章为 "Property"，显示 URI |
| GS-305 | Instance 类型展示 | 搜索含 Instance 结果 | 查看 Instance 结果项 | 图标为 database（蓝色），徽章为 "Instance"，显示 URI |
| GS-306 | Axiom 类型展示 | 搜索含 Axiom 结果 | 查看 Axiom 结果项 | 图标为 shield（红色），徽章为 "Axiom"，无 URI |
| GS-307 | 结果项分隔线 | 搜索有多个结果 | 查看结果列表 | 每个结果项之间有底部分隔线，最后一项无分隔线 |
| GS-308 | 结果项点击 | 搜索有结果 | 点击某个搜索结果 | 跳转至对应实体的详情页面 |

### 3.5 Mock 数据与结果数量

| 编号 | 用例名称 | 前置条件 | 测试步骤 | 预期结果 |
|------|---------|---------|---------|---------|
| GS-350 | Mock 数据至少 6 条 | 进入全局搜索 | 查看搜索结果列表 | 至少展示 6 条 mock 结果，涵盖 Class、Relation、Property、Instance、Axiom 不同类型 |
| GS-351 | 结果头部计数准确 | 进入全局搜索 | 查看 "Showing X of Y results" | 计数与实际展示的结果条数一致 |

### 3.6 跨模块检索

| 编号 | 用例名称 | 前置条件 | 测试步骤 | 预期结果 |
|------|---------|---------|---------|---------|
| GS-400 | 跨类型搜索 | 进入全局搜索 | 搜索 "Person" | 结果包含 Class（Person）、Instance（与 Person 相关）、Axiom（涉及 Person）等多种类型 |
| GS-401 | URI 搜索 | 进入全局搜索 | 搜索 "ent:hasEmployee" | 精确匹配到 hasEmployee 关系 |
| GS-402 | 描述内容搜索 | 进入全局搜索 | 搜索 "Datatype property" | 匹配到描述中含该关键词的结果（如 hasName） |
| GS-403 | 空结果处理 | 进入全局搜索 | 搜索不存在的关键词（如 "zzzznotexist"） | 显示空结果提示（如 "No results found"） |
| GS-404 | 特殊字符搜索 | 进入全局搜索 | 搜索包含特殊字符（如 "⊓", "⊑", "⊥"） | 正确处理特殊字符，匹配对应 Axiom |
| GS-405 | 大小写不敏感 | 进入全局搜索 | 分别搜索 "person" 和 "Person" | 两次搜索结果一致 |

### 3.6 分页与性能

| 编号 | 用例名称 | 前置条件 | 测试步骤 | 预期结果 |
|------|---------|---------|---------|---------|
| GS-500 | 结果分页 | 结果超过一页 | 查看分页/滚动加载 | 设计稿显示 "Showing 6 of 24 results"，应支持分页或滚动加载更多 |
| GS-501 | 加载状态 | 进入全局搜索 | 发起搜索请求 | 搜索过程中显示加载状态指示器 |
| GS-502 | 搜索响应速度 | 进入全局搜索 | 输入关键词后观察 | 搜索结果应在合理时间内返回（建议 < 2s） |

---

## 四、通用测试用例（适用于所有页面）

| 编号 | 用例名称 | 测试步骤 | 预期结果 |
|------|---------|---------|---------|
| COM-001 | 深色主题一致性 | 检查各页面整体配色 | 所有页面遵循深色主题，背景色、卡片色、文字色一致 |
| COM-002 | 字体一致性 | 检查各页面文字样式 | 主字体 Inter，标题 600 权重，正文 normal 权重 |
| COM-003 | 响应式布局 | 缩小浏览器窗口至不同尺寸 | 页面在 1440px 设计宽度下正常显示，窄屏下合理适配 |
| COM-004 | 侧边栏导航一致 | 切换不同页面 | 侧边栏结构和样式在各页面间保持一致 |
| COM-005 | 已登录用户信息 | 查看侧边栏底部 | 显示当前登录用户信息（头像、名称、角色） |
| COM-006 | Tab 键导航 | 使用 Tab 键遍历表单 | 焦点按合理顺序在各交互元素间切换 |
| COM-007 | 未授权访问 | 未登录状态访问受保护页面 | 重定向至登录页面 |

---

## 五、验收标准追溯矩阵

> 以下为产品经理验收标准与测试用例的对应关系

### Register Page 验收标准

| 验收项 | 对应用例 |
|--------|---------|
| `/register` 路由正确渲染 | REG-001 |
| 表单包含 4 个字段 | REG-002 |
| 空值禁止提交 | REG-100~104 |
| 密码不一致提示 | REG-107 |
| Create Account 跳转 `/login` | REG-200 |
| Sign In 跳转 `/login` | REG-204 |
| 与 LoginPage 视觉一致 | REG-008 |

### User Detail 验收标准

| 验收项 | 对应用例 |
|--------|---------|
| `/user-management/:userId` 路由渲染 | UD-001, UD-501 |
| MainLayout 布局 | UD-001 |
| 面包屑导航 | UD-003 |
| Edit / Delete 按钮 | UD-004 |
| 用户卡片完整展示 | UD-100~105 |
| 域卡片与统计数据 | UD-200~204 |
| Add Domain 按钮可点击 | UD-201, UD-206 |
| Mock 数据渲染 | UD-500~501 |

### Global Search 验收标准

| 验收项 | 对应用例 |
|--------|---------|
| `/search` 路由渲染 | GS-001 |
| 搜索栏含图标、placeholder、⌘K | GS-100 |
| 6 个筛选 Chip | GS-200 |
| Chip 切换选中状态 | GS-201~203 |
| 至少 6 条 mock 数据 | GS-350 |
| 结果展示图标、名称、badge、URI、描述 | GS-301 |
| 不同类型不同颜色 | GS-302~306 |
| 结果头部显示总数 | GS-300, GS-351 |

---

## 六、测试优先级

### P0（必须通过）— 验收标准核心项
- REG-001~002, REG-008, REG-100~108, REG-200, REG-204（注册核心流程与验收标准）
- UD-001~004, UD-100~103, UD-200, UD-500（用户详情核心展示与 mock 数据）
- GS-001, GS-100, GS-200~202, GS-300~301, GS-350（搜索核心功能与验收标准）

### P1（重要）
- REG-109~111, REG-201~203, REG-205~206, REG-300（注册验证与安全）
- UD-201~206, UD-300~307, UD-400~403, UD-501（域管理、编辑删除、列表衔接）
- GS-203~205, GS-302~308, GS-351, GS-400~405（筛选、类型区分、跨模块检索）

### P2（一般）
- REG-003~007, REG-301~302（UI 细节与安全深化）
- UD-104~105（UI 细节）
- GS-002~004, GS-101~105, GS-500~502（辅助功能与性能）
- COM-001~007（通用测试）

---

## 七、测试环境要求

| 项目 | 要求 |
|------|------|
| 浏览器 | Chrome (最新版)、Firefox (最新版)、Safari (最新版) |
| 分辨率 | 1440 x 900（设计稿基准）、1920 x 1080、1366 x 768 |
| 操作系统 | macOS、Windows |
| 网络环境 | 正常网络、弱网络、断网 |
| 测试账户 | Admin 角色账户、普通用户账户 |
